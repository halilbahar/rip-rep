import { SectionHostDirective } from './section-host.directive';

describe('SectionHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionHostDirective();
    expect(directive).toBeTruthy();
  });
});
